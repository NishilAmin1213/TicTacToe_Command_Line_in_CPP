#include <iostream>
#include <string>
using namespace std;

class player {
private:
    string name;

public:
    string tok;
    int num; //this num will be used to determine whose turn it is
    player(string, int, string);


};
